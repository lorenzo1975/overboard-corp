# SendGrid Configuration (Server-Side)
# These need to be created as secrets in Google Cloud Secret Manager
# and attached to your Firebase App Hosting backend.
SENDGRID_API_KEY="your_sendgrid_api_key"
SENDGRID_TO_EMAIL="your_to_address@example.com"
SENDGRID_FROM_EMAIL="your_from_address@example.com"

# Google Maps API Key (Client-Side)
# This value should be set in the `buildVars` section of apphosting.yaml
# for production deployments. For local development, you can add it to a .env.local file.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your_google_maps_api_key"
